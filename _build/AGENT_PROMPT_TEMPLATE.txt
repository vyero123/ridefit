Token-lean spec pull. For each model below, fetch EXACTLY ONE page: https://www.edmunds.com/<make>/<slug>/2026/features-specs/ — no searching, no other sites, no extra trims. Use the default ("Most Popular") trim. If the 2026 page does not exist, skip it and list it in notes. If a field is missing on the page, leave it out.

<MAKE> slugs: <SLUGS>

From the page take, inches / pounds / cu ft: height, length, width ("Overall width without mirrors"), wheelbase, clearance, weight ("Curb weight"), tire (the "NNN/NNRNN tires" line; if the rim size is missing, append the wheel diameter, say so in src and put "tire" in approx), seats, headroom1/legroom1/shoulder1/hip1 (front), headroom2/legroom2/shoulder2/hip2 (rear), headroom3/legroom3 (third row if listed), cargo1 ("Maximum cargo capacity"), cargoLast ("Cargo capacity, all seats in place"), towing ("Maximum towing capacity", number only). rows = 3 if a third row is listed, 1 for two-seaters, else 2. Put cargoLast into cargo2 for 2-row vehicles and into cargo3 for 3-row vehicles. bodyClass: sedan|coupe|hatchback|liftback|crossover|body-on-frame-suv|minivan|cargo-van|roadster; template: sedan|coupe|hatchback|suv|minivan|van (roadster/convertible -> coupe, liftback -> hatchback). name = default trim name, shortened. year 2026. src = "Edmunds 2026 <Make> <model> features & specs (<trim>)".

WRITE the JSON array to the file <RAW_PATH> using your file-writing tool (this saves the caller from retyping it), then reply with only the {"notes":"..."} line. The array is of objects {brand, model, name, year, template, bodyClass, height, length, width, wheelbase, clearance, weight, tire, seats, rows, headroom1, legroom1, shoulder1, hip1, headroom2, legroom2, shoulder2, hip2, headroom3, legroom3, cargo1, cargo2, cargo3, towing, approx, src} 
FREE-RIDER EQUIPMENT (no extra fetches, no extra pages): if the SAME page you already fetched lists standard/optional features for the default trim (Edmunds sections "Safety", "In-Car Entertainment", "Power Features", "Comfort & Convenience", "Front/Rear Seat Features", "Tires & Wheels", "Packages"), add "equipment": {"<trim name>": {"standard": ["heated front seats", "wireless Apple CarPlay/Android Auto", ...], "optional": ["panoramic sunroof", ...]}} with short lowercase feature names taken verbatim-ish from the page (dedupe, skip pure spec numbers). Current model year only. If the page shows none, omit the key.

then one {"notes":"..."} line. JSON only.
